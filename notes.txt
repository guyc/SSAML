Process:

SSAML:  Spreadsheet Abstraction Markup Language

Broadest view:

Parse a SSAML string into a tree
Generate PHP from the tree         
Run the PHP to generate SSXML
Process the SSXML to generate XMLX doc with PHPExcel

SSAML:

%workbook
  %sheet(name="")
    %column(col="a")
    %row(row="1")
      %cell(colspan="2" rowpan="2")
        SPANNED

